// This page is intentionally left empty because the redirect is handled at the server level 
// using the `redirects` field in next.config.mjs.
// When users visit `/`, they are automatically redirected to `/products/1`.

export default function Home() {
  return null;
}
